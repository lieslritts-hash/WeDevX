package week5;

/*
5.25.2 Complex count (with solution)
easy
|
30 min
|
Share
Write a method called countCharacterTypes() that takes a String as a parameter. The method should count the number of alphabetic characters, digits, whitespace characters, and special characters (non-alphabetic, non-digit, and non-whitespace) in the string. The counts should be stored in an array of integers and returned. To count different types of characters, use Character class with the learned methods:

To count alphabetic characters - isLetter(ch);

To count digit characters - isDigit(ch);

To count whitespace characters - isWhitespace(ch);

To count the number of other types of characters, you can use else statement.

Return Type: int[] - an array of integers representing the count of each character type.

Example #1:
Parameter:

plain
Copy code
String str = "Hello 123 World!"
Return:

plain
Copy code
[10, 3, 2, 1]
// The input string contains 10 alphabetic characters, 3 digits,
2 whitespace characters, and 1 special character.
Example #2:
Parameter:

plain
Copy code
String str = ""
Return:

plain
Copy code
[0, 0, 0, 0]
: If the input string is empty, the method should return an array of zeros

Video solution:
https://youtu.be/IVXOmSiyujI
 */
//write your static method here. No need to add Main class or main method.

public class Week5_25_2_ComplexCount {
    public static void main(String[] args) {

    }
}
